/**
 * File:   metodosFundamentais.c
 * Author: Danton Issler Rodrigues
 *
 * Created on March 29, 2019, 2:45 PM
 */

#include <stdio.h>
#include <stdlib.h>
#include "metodosRemocao.h"

void removeListaInicio(Elem *li){
//    if(li == NULL)
//        return 0;
//    if((*li) == NULL)
//        return 0;banco
//    Elem *no = *li;
//    *li = no->prox;
//    if(no->prox != NULL)
//        no->prox->ant = NULL;
//    free(no);
}
void removeListaFim(Elem *li){
//    if(li == NULL)return 0;
//    if((*li) == NULL)
//        return 0;
//    Elem *no = *li;
//    while(no->prox != NULL)
//        no = no->prox;
//    if(no->ant == NULL)//Remover o primeiro e unico
//        *li = no->prox;
//    else
//        no->ant->prox = NULL;
//    free(no);
}
void removeListaMeio(Elem *li, int conta){
//    if(li == NULL)return 0;
//    Elem *no = *li;
//    while(no != NULL && no->dados.numConta != mat){
//        no = no->prox;
//    }
//    if (no == NULL)//Não encontrado
//        return 0;
//    if(no->ant == NULL)//Remover o primeiro?
//        *li = no->prox;
//    else
//        no->ant->pro = no->prox;
//    if(no->prox != NULL)
//        no->prox->ant = no->ant
//    free(no);
}
